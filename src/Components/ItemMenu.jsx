export default function ItemMenu(props) {
    return (
        <>
            <ul>
                <span>{props.icone}</span>
                <span>{props.texto}</span>
            </ul>
        </>

    )
}